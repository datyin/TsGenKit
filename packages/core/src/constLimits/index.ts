/**
 * The highest supported number in JavaScript.
 *
 * @category Number
 *
 * @example
 * ```ts
 * import { MAX_NUMBER } from "@datyin/core";
 * 
 * console.log(MAX_NUMBER); // 1.7976931348623157e+308
 * ```
 */
const MAX_NUMBER = 1.7976931348623157e+308;

/**
 * The lowest supported number in JavaScript.
 *
 * @category Number
 *
 * @example
 * ```ts
 * import { MIN_NUMBER } from "@datyin/core";
 *
 * console.log(MIN_NUMBER); // -1.7976931348623157e+308
 * ```
 */
const MIN_NUMBER = -1.7976931348623157e+308;

/**
 * The maximum number of decimal places allowed in a number.
 *
 * @category Number
 *
 * @example
 * ```ts
 * import { MAX_DECIMAL_PLACES } from "@datyin/core";
 * 
 * console.log(MAX_DECIMAL_PLACES); // 20
 * ```
 */
const MAX_DECIMAL_PLACES = 20;

export { MAX_NUMBER, MIN_NUMBER, MAX_DECIMAL_PLACES };
